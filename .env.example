# Server-only. Do not prefix this with VITE_ — that would ship the key to the browser.
# Create a key at https://aistudio.google.com/apikey then copy this file to .env.local
GEMINI_API_KEY=
# Optional. Defaults to gemini-3.6-flash
# GEMINI_MODEL=gemini-3.6-flash
